exports.signup = (req, res, next) => {
  // see if user with email exists
  console.log(req.body);

  // if user with email exists return an error

  // if email does not exist, create and save

  // respond to request
  res.send({ success: true });
};
